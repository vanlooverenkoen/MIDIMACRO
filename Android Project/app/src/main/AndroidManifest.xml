<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.vanlooverenkoen.midimacro">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.software.midi"
        android:required="true" />

    <application
        android:name=".MidiMacro"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/title_app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.SplashScreenActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.MainActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_app"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.SettingsActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".ui.AboutActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/tile_activity_about" />
        <activity
            android:name=".ui.LicenceActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/tile_activity_license" />
        <activity
            android:name=".ui.AppIntroActivity"
            android:label="@string/title_activity_app_intro"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>
</manifest>